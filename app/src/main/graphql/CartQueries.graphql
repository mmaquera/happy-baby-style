# Cart Queries
# These queries handle shopping cart data retrieval

query GetUserCart($userId: ID!) {
  userCart(userId: $userId) {
    id
    userId
    createdAt
    updatedAt
  }
}

query GetCartItem($id: ID!) {
  cartItem(id: $id) {
    id
    createdAt
    updatedAt
  }
}
